using System;
//using Core.CrudTools.Business;

namespace Core.Promsy.Models.Export
{
    /*
     * public static class ExceptionExtensions
    {
        public static void BusinessErrorLog(this Exception exception)
        {
            try
            {
                BusinessObject.ObtenerLogger.Fatal("Excepcion: " + exception.Message);

                BusinessObject.ObtenerLogger.Fatal("StackTrace: " + exception.StackTrace);
                if (exception.InnerException != null) exception.InnerException.BusinessErrorLog();
            }
            catch
            {
                BusinessObject.ObtenerLogger.Fatal("Error tratando de mostrar el error");
            }
        }
    }
    */
}